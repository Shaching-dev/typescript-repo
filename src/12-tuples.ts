// let user: [string, number];
// user = ["hello", 121];

const ess = ["263256", 32132];
const userEntry: [string, number] = ["shaching", 1213];
userEntry[1] = 4234;

type Res = [status: number, message?: string];
const r11: Res = [200, "Response success"];

// readonly tupples
const corners: readonly [number, string] = [121, "sahdus"];

// corners.map((cor) => console.log(cor));

// function greet(name: string) {
//   return "Hello " + name;
// }

// greet(10)
